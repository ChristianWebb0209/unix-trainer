# Systems workspace: Bash, Awk, Shell, C, C++, Rust
FROM alpine:latest

RUN apk add --no-cache \
    bash \
    gawk \
    coreutils \
    nano \
    vim \
    build-base \
    rust \
    cargo

WORKDIR /workspace

CMD ["tail", "-f", "/dev/null"]
